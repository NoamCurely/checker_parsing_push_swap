
# Checker Parsing Push swap
The checker parsing push swap allows you to verify the different inputs that you must manage or not.

It’s a bash script with simply echo


## Run Locally

Clone the project

```bash
   git clone https://github.com/NoamCurely/checker_parsing_push_swap.git
```

Go to the project directory

```bash
  cd my-project
```

give the permissions to the file

```bash
  chmod +x checker.bash
```

Start the checker

```bash
  ./checker.bash
```

